// Hear will import both user Navbars and will create a condition that will change the navbar when the width changes

import React from "react";

const UserNavbar = () => {
  return <>User Navbar</>;
};

export default UserNavbar;
